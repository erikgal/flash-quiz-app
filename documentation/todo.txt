TASKS:

- Add multiple choice quiz play screens

- In the PreviewScreens (both home and store), add some kind of way to preview/reveal 
the questions

- Only be able to upload quizes with corresponding creatorId

- Add a raiting system (1-5 stars) and download count

- Add a shuffle mode which directle utilizes an api

- Add search functionality in both home and store with andvanced filter functionality



BUGS:

- If you log out from a user who has som quizzes, and than sgin up a new account, 
an infinte loading happens, add a clean up side effect??

- In SignUpScreen there is an async issue with () => setSecureTextEntry(!secureTextEntry)

